"""Seed Alice, Bob, Carol across Cognee + pgvector + TensorZero.

Run via ``make seed`` (or ``uv run python -m examples.fastapi_demo.backend.seed``).

Lands Day 6 — exercises the full vismaran_sdk surface so the orchestrator has
something to delete.
"""
